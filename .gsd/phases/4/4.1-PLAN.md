---
phase: 4
plan: 1
wave: 1
---

# Plan 4.1: Real-time Infrastructure & API Setup

## Objective
Setup the Supabase client on the backend using the service role key for publishing to Broadcast, and implement the Chat API endpoints.

## Context
- .gsd/SPEC.md
- .gsd/DECISIONS.md
- apps/api/src/lib/realtime.ts (to create)
- apps/api/src/routes/chat.ts (to create)
- apps/api/src/index.ts

## Tasks

<task type="auto">
  <name>Setup Backend Supabase Client</name>
  <files>apps/api/src/lib/realtime.ts, apps/api/.env.example</files>
  <action>
    - Do not recreate `apps/api/src/lib/realtime.ts` - if it already exists. Import the existing supabase client and pushNotification helper from it.
    - If it needs modification, ensure it uses `@supabase/supabase-js` with `process.env.SUPABASE_URL` and `process.env.SUPABASE_SERVICE_KEY` (or anon key if service key is missing).
    - Ensure `.env.example` includes `SUPABASE_SERVICE_KEY`.
  </action>
  <verify>grep "createClient" apps/api/src/lib/realtime.ts</verify>
  <done>realtime.ts exports a configured supabase client.</done>
</task>

<task type="auto">
  <name>Implement Chat API Endpoints</name>
  <files>apps/api/src/routes/chat.ts, apps/api/src/index.ts</files>
  <action>
    - Create `apps/api/src/routes/chat.ts`.
    - Implement `GET /api/chat/:chatId/messages`: Fetch historical messages from Prisma `Message` table, ordered by `createdAt` desc (or asc, depending on UI needs). Ensure the user (`req.user`) is a member of a Team associated with this chat or is directly in a DM chat. ADD CURSOR PAGINATION: Add `?cursor=<messageId>` query param support (take: 30, skip: cursor ? 1 : 0, cursor: cursor ? { id: cursor } : undefined).
    - Implement `POST /api/chat/:chatId/messages`: Validate user is part of the chat. Save message to Prisma `Message` table.
    - After saving, use the Supabase client to publish a message event to the Broadcast channel `chat:${chatId}`. The event should contain the message payload.
    - Register the chat router in `apps/api/src/index.ts` under `/api/chat` and ensure it requires authentication.
  </action>
  <verify>npm run build -w @hackmatch/api</verify>
  <done>Chat API compiles successfully and implements both endpoints with broadcast functionality.</done>
</task>

## Success Criteria
- [ ] Backend Supabase client is configured (using existing if present).
- [ ] Chat API allows retrieving and posting messages with cursor pagination.
- [ ] Posting a message broadcasts to Supabase Realtime.
- [ ] checkpoint:human-verify - User cannot access a chat they are not a member of (returns 403)
- [ ] checkpoint:human-verify - Historical messages load correctly on chat open before realtime kicks in
